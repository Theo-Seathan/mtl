import { MyInputTextDirective } from './my-input-text.directive';

describe('MyInputTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MyInputTextDirective();
    expect(directive).toBeTruthy();
  });
});
